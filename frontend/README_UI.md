# DevPilot Frontend UI (Mocked)

> AUTOGENERATED BY COPILOT PROMPT â€“ replace mocks with real API calls at TODO comments.

## Quick verification
1. Run dev server: `pnpm --dir frontend dev` then open http://localhost:5173/jobs.
2. Check the Shell sidebar/topbar renders.
3. Jobs page shows mock jobs (not a loading spinner forever); click a row to reach `/jobs/:id`.
4. Toggle theme button; reload to confirm persistence.
5. On Job Detail, AI review markdown renders with code block copy; logs panel toggles and copy works.
6. Workers page lists mock workers and metrics; restart buttons stay disabled in mock.
7. If markdown libs are missing, install: `pnpm --dir frontend add react-markdown rehype-highlight`.

## What to replace for real data
- Swap `useMockJobs` in `src/hooks/useMockJobs.ts` with TanStack Query `useQuery('/api/jobs')` and wire sockets for live updates (TODO markers included).
- Replace mocked worker data in `WorkersPage` with real API calls and websocket health feeds.
- Ensure `MarkdownRenderer` sanitizes any untrusted HTML if server-side rendering is introduced (TODO note inline).

## Notes
- Tailwind config moved to `tailwind.config.ts` and global styles live in `src/styles/globals.css`.
- System font stack, neutral palette, and light/dark themes are handled via `ThemeProvider` and CSS variables.
- For screenshots: Jobs â†’ click any row, toggle logs in Job Detail, and switch themes from the topbar.

